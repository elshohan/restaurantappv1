import 'package:flutter/material.dart';

class KartShopScreen extends StatelessWidget {
  const KartShopScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Kart Shop Screen'),
    );
  }
}
